{
  "buildCommand": "pnpm --filter @workspace/craizeg run build",
  "outputDirectory": "artifacts/craizeg/dist/public",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
